import { TaskCommandCenter } from "./_components/task-command-center";

export const metadata = { title: "Tasks — Polymath" };

export default function TasksPage() {
  return <TaskCommandCenter />;
}

"use client";
// TODO(T6): kind/status/tag/search + "Todos only" toggle
export function EntryFilterBar() {
  return (
    <div className="text-xs uppercase tracking-widest font-mono text-[var(--alfred-text-tertiary)]">
      FILTER BAR — pending T6
    </div>
  );
}

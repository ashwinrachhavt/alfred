"use client";

import type Sigma from "sigma";
import type Graph from "graphology";

/**
 * Placeholder — Task 13 replaces this with a real convex-hull overlay using d3-polygon.
 * This stub exists so NexusGraph's import resolves at type-check time.
 */
type ClusterHullLayerProps = {
  sigma: Sigma | null;
  graph: Graph;
  enabled: boolean;
};

export function ClusterHullLayer(_props: ClusterHullLayerProps): null {
  return null;
}

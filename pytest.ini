[pytest]
testpaths = tests
addopts = -q -m "not integration"
markers =
    integration: tests that require external services (e.g., Mongo); skipped by default
    network: tests that intentionally perform network IO (should be rare)

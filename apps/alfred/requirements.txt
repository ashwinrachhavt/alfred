# This project now manages dependencies with uv.
# Use `uv sync --dev` to install the environment, or `uv export > requirements.txt`
# to regenerate pinned requirement snapshots.
-r ../../requirements.txt
